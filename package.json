{
  "name": "alex-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Different Thinking Alex prototype with Gemini-backed chat.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
